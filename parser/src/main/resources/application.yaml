spring:
  datasource:
    url: jdbc:mysql://localhost:3306/mydb?useSSL=false&serverTimezone=UTC
    username: admin
    password: admin
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect  # Explicitly set the dialect
    hibernate:
      ddl-auto: none
  profiles:
    active: dev
frontend:
  url: http://localhost:4200/
parser:
  threads-amnt: 1